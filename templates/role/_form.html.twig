{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-end gap-1">
        <button class="btn btn-primary"><i class="bi bi-pencil-square m-1"></i>{{ button_label|default('Save') }}</button>
        {{ include('role/_delete_form.html.twig') }}
    </div>
{{ form_end(form) }}
